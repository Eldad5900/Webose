# Webose — Vite + React + TypeScript

Scaffolded minimal Vite + React + TypeScript project.

Quick start:

```bash
cd /Users/eldadlavi/webose
npm install
npm run dev
```

Notes:
- Files of interest: `src/main.tsx`, `src/App.tsx`.
- Build with `npm run build` and preview with `npm run preview`.
